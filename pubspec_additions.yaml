# أضف هذه Dependencies إلى pubspec.yaml الخاص بك

dependencies:
  flutter:
    sdk: flutter
  
  # المكتبات الموجودة مسبقاً
  go_router: ^14.0.3
  intl: ^0.19.0
  url_launcher: ^6.3.0
  flutter_dotenv: ^5.1.0
  supabase_flutter: ^2.5.6
  flutter_map: ^6.2.1
  latlong2: ^0.9.1

  # المكتبات الجديدة المطلوبة ⬇️
  flutter_riverpod: ^2.4.0      # ← State Management
  shared_preferences: ^2.2.0     # ← التخزين المحلي
  file_picker: ^6.0.0            # ← اختيار الملفات
  path_provider: ^2.1.0          # ← مسارات الملفات

# بعد إضافة المكتبات، نفذ:
# flutter pub get
